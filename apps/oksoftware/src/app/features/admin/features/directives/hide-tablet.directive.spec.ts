import { HideTabletDirective } from './hide-tablet.directive';

describe('HideTabletDirective', () => {
  it('should create an instance', () => {
    const directive = new HideTabletDirective();
    expect(directive).toBeTruthy();
  });
});
