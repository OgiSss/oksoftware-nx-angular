import { IpadResponsiveDirective } from './ipad-responsive.directive';

describe('IpadResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IpadResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
